<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}MyBookstore - Votre Librairie en Ligne{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>📚</text></svg>">
        
        {# Bootstrap 5 #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        {# FontAwesome #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        
        <style>
            :root {
                --primary-color: #007bff;
                --secondary-color: #6c757d;
            }
            
            body {
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                background-color: #f8f9fa;
            }
            
            nav {
                box-shadow: 0 2px 4px rgba(0,0,0,.1);
            }
            
            main {
                flex: 1;
            }
            
            footer {
                background-color: #212529;
                color: white;
                padding: 40px 0 20px;
                margin-top: 60px;
                border-top: 1px solid #dee2e6;
            }
            
            .btn-primary {
                background-color: var(--primary-color);
                border-color: var(--primary-color);
            }
            
            .btn-primary:hover {
                background-color: #0056b3;
                border-color: #0056b3;
            }
            
            .card {
                border: none;
            }
            
            .alert {
                border: none;
                border-radius: 0.5rem;
            }
            
            .table {
                margin-bottom: 0;
            }
        </style>

        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {% include 'partials/_navbar.html.twig' %}
        
        <main>
            <div class="container-fluid">
                {% for type, messages in app.flashes %}
                    {% if type == 'success' %}
                        {% set alertClass = 'alert-success' %}
                    {% elseif type == 'error' %}
                        {% set alertClass = 'alert-danger' %}
                    {% elseif type == 'warning' %}
                        {% set alertClass = 'alert-warning' %}
                    {% else %}
                        {% set alertClass = 'alert-info' %}
                    {% endif %}
                    
                    {% for message in messages %}
                        <div class="alert {{ alertClass }} alert-dismissible fade show mt-3" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>

            {% block content %}{% endblock %}
        </main>

        <footer class="mt-5">
            <div class="container">
                <div class="row">
                    <div class="col-md-4 mb-4">
                        <h5 class="fw-bold">MyBookstore</h5>
                        <p class="text-muted">Votre librairie en ligne pour découvrir des milliers de livres.</p>
                    </div>
                    <div class="col-md-4 mb-4">
                        <h6 class="fw-bold">Liens rapides</h6>
                        <ul class="list-unstyled">
                            <li><a href="{{ path('app_home') }}" class="text-muted text-decoration-none">Accueil</a></li>
                            <li><a href="{{ path('app_catalogue') }}" class="text-muted text-decoration-none">Nos Livres</a></li>
                            {% if app.user %}
                                <li><a href="{{ path('app_profile') }}" class="text-muted text-decoration-none">Mon Profil</a></li>
                            {% endif %}
                        </ul>
                    </div>
                    <div class="col-md-4 mb-4">
                        <h6 class="fw-bold">Contact</h6>
                        <p class="text-muted small">
                            Email: contact@mybookstore.com<br>
                            Tel: +33 1 23 45 67 89
                        </p>
                    </div>
                </div>
                <hr class="bg-secondary">
                <div class="text-center text-muted">
                    <p>&copy; 2024 MyBookstore. Tous droits réservés.</p>
                </div>
            </div>
        </footer>

        {# Bootstrap JS #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
